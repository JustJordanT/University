@page
@model University.Students.UI.Pages.Students.EditModel
@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>
<a asp-page="./Index">Back to List</a>
<br />

<div class="pt-3">
    <form method="post">
        @* <div class="form-group"> *@
        @*     <label asp-for="Student.Id"></label> *@
        @*     <input asp-for="Student.Id" class="form-control" readonly/> *@
        @* </div> *@
        <div class="form-group mb-3">
            <input asp-for="Student.Name" class="form-control" placeholder="Name" />
        </div>
        <div class="form-group mb-3">
            <input asp-for="Student.Email" class="form-control" placeholder="Email"/>
        </div>
        <div class="form-group mb-3">
            @* <label asp-for="Student.Age"></label> *@
            <input asp-for="Student.Age" class="form-control" placeholder="Age" />
        </div>
        <br />
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
</div>
